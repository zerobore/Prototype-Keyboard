<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Base Activity theme (Compose draws the actual UI via Material3). -->
    <style name="Theme.ProtoKeyboard" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/window_background</item>
        <item name="android:statusBarColor">@color/window_background</item>
    </style>
</resources>
